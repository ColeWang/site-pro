import { defineComponent } from 'vue'
import { Text } from '../../src'

export default defineComponent({
    setup () {
        return () => {
            return (
                <Text/>
            )
        }
    }
})
