import Table from './Table'
import type {
    TableColumn,
    TableExpose,
    TableInstance,
    TableProps,
    TableScroll,
    TableSharedContext,
    TableSize,
    TableSlots
} from './typings'
import { createTableColumn, tableProps } from './typings'

export { createTableColumn }
export { Table, tableProps }

export type { TableColumn, TableScroll, TableSize, TableSharedContext }
export type { TableSlots, TableProps, TableExpose, TableInstance }
